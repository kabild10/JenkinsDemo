<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CI/CD Pipeline Success</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #141E30, #243B55);
      color: #fff;
    }

    .card {
      background: rgba(255, 255, 255, 0.12);
      backdrop-filter: blur(12px);
      border-radius: 20px;
      padding: 3rem;
      width: 90%;
      max-width: 520px;
      text-align: center;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.35);
      animation: fadeIn 1.2s ease;
    }

    h1 {
      font-size: 2.3rem;
      margin-bottom: 1rem;
      color: #00e5ff;
    }

    p {
      font-size: 1.1rem;
      margin-bottom: 1.8rem;
      color: #e0e0e0;
    }

    .badge {
      display: inline-block;
      background: #00c853;
      padding: 0.5rem 1.2rem;
      border-radius: 50px;
      font-size: 0.95rem;
      font-weight: 600;
      color: #fff;
      box-shadow: 0 4px 10px rgba(0, 200, 83, 0.5);
    }

    footer {
      margin-top: 2rem;
      font-size: 0.85rem;
      color: #b0bec5;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <div class="card">
    <h1>🚀 CI/CD Pipeline Success</h1>
    <p>Your Jenkins pipeline executed and deployed this page successfully.</p>
    <div class="badge">BUILD SUCCESS</div>
    <footer>
      Powered by Jenkins • Automated Deployment
    </footer>
  </div>
</body>
</html>
